import React from 'react';
import { Typography } from '@material-ui/core';

const TellUsAbout = (props) => {
  return <Typography variant='h6'>{props.description}</Typography>;
};

export default TellUsAbout;
